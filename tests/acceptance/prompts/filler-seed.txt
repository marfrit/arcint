A gated delta network folds every token it has seen into a fixed-size recurrent state, trading exact recall for a bounded memory footprint, which is the opposite trade a paged attention cache makes: the cache keeps every token's key and value exactly, at the cost of memory that grows with the sequence. Explain, in careful and precise prose, why a state built this way cannot be truncated the way a key-value cache can be, and be specific about what information is irrecoverably mixed together once it has been folded in.
