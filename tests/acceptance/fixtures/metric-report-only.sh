#!/usr/bin/env bash
# Far below the recorded 20.0 -- a gated reference would REGRESS here; a
# report-only one (gate_at null) records it and never fails the run.
echo "ACCEPTANCE-METRIC fake-prefill 5.0 t/s"
