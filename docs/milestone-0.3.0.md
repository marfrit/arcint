# 0.3.0 — the extension series (M7–M14)

0.2.x closed the correctness line: M0–M6 are done, the engine serves two cards,
the gates in DESIGN §5 hold, and every number in DESIGN §7 names its card and
configuration. 0.3.0 is the performance-extension line — the features that sit
on top of a correct engine the way [ik_llama.cpp](https://github.com/ikawrakow/ik_llama.cpp)
sits on top of llama.cpp: quant mixes, offload policy, KV codecs, drafting,
auto-fit. Writing OpenVINO plugin patches is **in scope** for this series; the
`patches/` series already ships in the source-built plugin package and this
plan adds to it.

## Ground rules

1. **Every item ends in a measurement**, with the card, depth, KV precision and
   configuration named. The §5 gates apply unchanged: Prüfstand 10/10 on the
   coder artifact, byte-exact equivalence wherever the change claims to be
   invisible, red case first. DESIGN §5.1 names which of unit, milestone or
   acceptance cadence a given check belongs to; a release checklist names its
   acceptance set from there, not from a new list.
2. **Plugin patches need a fusion-impact profile**, not a kernel
   micro-benchmark. The 8.6×-faster custom kernel that lost 1.6 ms end-to-end
   (DESIGN §8) is the standing warning: a patch is judged by what the graph
   optimiser does around it.
3. **No item closes as "hardware baseline reached" without a survey.** Closing
   a performance milestone requires a recorded look at what other stacks do on
   the same silicon — upstream OpenVINO release notes, vLLM/SGLang XPU,
   ik_llama.cpp's CPU-side tricks, independent testbeds — not just our own
   numbers. The one time this repository assumed the hunt was over, the answer
   was a 44.8 t/s drafter someone else had already trained. A clean survey
   ("nobody does better on this path, here is what I checked") is a valid
   close; an unsurveyed one is not.

## Where the bar sits (survey, 2026-09-01)

| stack | result on comparable silicon | source |
|---|---|---|
| arcint 0.2.12 | coder int4: 71.3 t/s plain / 44.8 t/s DFlash-assisted agent on the B60; 24.0→44.8 t/s drafter gain; 35B at `--offload-ratio 20` on the A770: ~1.9 t/s | DESIGN §7.0.2r/s |
| vLLM XPU | 83.5 t/s, 20B model, 2× Arc B580 | xda-developers survey |
| upstream OpenVINO 2026.2 | MoE disk offload, batched-MoE decode optimisation, PagedAttention tree-mask (EAGLE-3), XMX dynamic quantisation | release notes |
| ipex-llm (archived 2026-01) | FlashMoE served 235B-class MoE on 1–2 Arc cards before archival | project README |
| DFlash-TfM (Mirai Labs) | τ 8–10 tokens/cycle with tree drafting vs 2.67 chain, same DFlash checkpoint family (B200, SGLang) | arXiv:2607.06763 |

Two entries in that table are direct evidence against "we are done": the tree
drafting τ, and upstream's own MoE decode work landing in the very plugin we
patch.

## The template, mapped

| ik_llama.cpp feature | arcint 0.3.0 analog |
|---|---|
| auto-fit offloaded tensors to VRAM, per-GPU margin | M7 |
| quantized KV (`q8/q6/q4`), Hadamard K/V transforms | M8 |
| `--cpu-moe`, tensor overrides, hybrid offload strategy | M9 |
| SOTA sub-4-bit quants (IQ_K, trellis), custom quant mixes | M10 |
| ngram/suffix self-speculation, DFlash, MTP | M11 (MTP/DFlash already served in 0.2.x) |
| iqk AVX2/AVX-512 GEMM kernels (150–350% CPU prompt speedup vs upstream) | M14 |
| fused MoE FFN (`-fmoe`) | M12 (exporter lowering so `fuse_moe_experts` fires) |
| — (llama.cpp: `--no-mmproj-offload`) | M13 |
| MLA/FlashMLA, Bitnet, tensor parallel, sampler zoo | deliberately excluded (below) |

## Milestones

| # | milestone | exit criterion |
|---|---|---|
| M7 | **Auto-fit and the honest reservation** — trial-allocate weights, expert slots and KV at load (deferred commit); on failure, shrink and **replay** the placement until it fits; print a max context that is true | With `--n-ctx` omitted, the printed max context loads *and completes a generation at full depth* on both cards, with and without `--offload-ratio`; the 0.2.12 "reservation under-counts" warning is deleted because the condition is gone; promised-vs-actual free memory error ≤ 2%. **2026-09-02: met** — auto-adopt served at 155,488 vs the hand-tuned 155,648 on the B60 (one live overshoot-correct pass); 0.8% promised-vs-actual on the A770 at ratio 20; the slot pool measured host-side (GTT), so the reservation keeps device and host ledgers (DESIGN §7.0.2t) |
| M8 | **Asymmetric paged KV: q8 keys, q4 values** — plugin patch: PagedAttention takes per-tensor cache precision (u8 K / i4 V) instead of one for both; keys hold the rope structure and are the sensitive half | Prüfstand 10/10 on the coder (named card, u8-K/i4-V), KV bytes/token reduction measured (the −36% row figure predates the padding arithmetic — real span −18…25%, §o-m8 design), decode within 5% of u8/u8 at 32k depth; a KLD-style probe against u8/u8 recorded. Stretch, same slot: the int8 group-64 codec with fused Hadamard pre-rotation (DESIGN §8), with the required fusion-impact profile. **2026-09-02: arcint groundwork landed** — `--paged-kv KEY[:VALUE]`, bitwidth-true cost model (fixed a 2× i4 over-count and a silent env fallback), `VALUE_CACHE_PRECISION` plumbing with the fail-loud ladder proven on-card ("Option not found … refusing"), per-port bitwidth audit (u8↔i8 alias measured and accepted). Symmetric `u4` serves and prices (6.3 vs 11.3 KiB/token, 35B). **Reduced to asymmetric REFUSAL** (§7.0.2w): the decode kernels derive both operands' packing from one config value, so u8:i4 can only fail loud (proven on-card), not serve — the durable win is the arcint ladder. **2026-09-02 (reversed): SERVES.** The parked verdict was overturned — the per-side kernel split is small, and the u8 garbage was two real source bugs (signed-i8 value read; unsplit write kernel), both fixed and CPU-reference-verified (§7.0.2w). u8:i4 serves at 8.8 vs 11.3 KiB/token (−22%); 210 paged_attention unit tests green; patches 0008/0009/0010 promoted to the applied series. Owed: u8:i4 prefill throughput price, cold/warm prefix byte-exactness; production still +p1. **2026-09-02, one gate discharged (DESIGN §7.0.2y):** on the served coder artifact (16 GiB card, one lane, no offload) `--paged-kv u8:i4` auto-fits 171,312 tokens against 133,456 at u8 (+28.4%, within 0.1 pp of the cost model) and scores 10/10 on the acceptance task; on the 24 GB card the dense agent model auto-fits 199,424 vs 155,376 (+28.3%); greedy text identical u8 vs u8:i4 at 16 tokens on both cards. That is the acceptance gate for the 0001–0012 series in the production coder unit's configuration; the owed items above (prefill price, prefix byte-exactness, the row's 32k decode and KLD probes) stay owed, and deployment remains a separate decision. **2026-09-03, prefill price measured (§7.0.2aa):** u8:i4 prefill +7% / +25% / +72% of prefill time at 8.9k / 37.7k / 71.7k tokens on the coder (decode at parity); the u8:i4 prefill faults at the 119k-token prompt that u8 prefills, and passes at 71.7k — bounded and disclosed; the opt path's depth-scaling scratch is the reading, the fix candidates are named and a chunk-size discriminator is queued. **2026-09-04, scratch charged and the crash disclosed (DESIGN §7.0.2ab/§7.0.2ac; CHANGELOG "Known defects"):** the u8:i4 prefill scratch the discriminator above named is now charged by the fit itself — on the 16 GiB card auto-fit adopts 102,384 tokens at chunk 64 unbounded, and 165,680 tokens at chunk 128 once patch 0015's bound (`--paged-attention-max-partitions 32`) is in effect, charging 48.5 MiB of scratch. Patch 0015 (bounded partials, f16 host sizing, `PAGED_ATTENTION_MAX_PARTITIONS`, a forced kernel-argument rebind after intermediate-buffer reallocation) measured: the plugin's 220 paged-attention unit tests pass, bounded and unbounded are byte-identical at the same chunk, acceptance scores 10/10 (unbounded on both cards, bounded on the 24 GB card), and the patched plugin at the bound serves the 119k-token prompt from a 131,072-token pool that the unpatched plugin cannot. CLOSED (DESIGN §7.0.2ad): the deep-prompt crash on the 24 GB card is a diagnosed driver/runtime interaction, not a plugin defect. RETRACTED: the earlier depth-triggered framing ("past 98k tokens", "384 partitions") was wrong — the trigger is one fixed 98,147-token prompt, crash depth within its prefill is random (~3.6k/12k/40k tokens on three crashes), and the actual trigger is VRAM headroom plus concurrent host or other-card load, not depth; and the "119k" name used for this same prompt elsewhere in this row was itself wrong — 331,500 chars = 98,147 tokens, not 119k. Mechanism: every crash is a page-fault storm at the OpenCL runtime's own direct-submission semaphore buffer (one fixed GPU virtual address, identified in the runtime's own allocation log), evicted as an ordinary user BO under VRAM pressure and never revalidated before its next wait; the plugin's paged-attention kernels are exonerated (0 stale bindings over 5,867 traced dispatches). Every crash on the 24 GB card on this record used the `ARCINT_PREFILL_CHUNK_CAP=off` diagnostic bypass; with `ARCINT_PREFILL_CHUNK_CAP` at its default (the only default in this cell — the bound was set explicitly, `--paged-attention-max-partitions 32`; the stock default, bound 0, with the term active is unmeasured here), the fit's scratch term active on the 24 GB card, u8:i4, `--n-ctx 131072`, passed 2 of 2 runs at the same cell. `NEOReadDebugKeys=1 EnableDirectSubmission=0` is documented as a fallback if the bound is ever violated (3/3 patched, 2/2 untouched, 5/5 on the 16 GiB card at +1.4% wall) — but its control also passes on a quiet host, so that evidence is mechanism-derived, not a rate result. Matches open upstream `drm/xe` item #8390 (same runtime heap region, this card's generation) plus a second candidate mechanism for the SAME fault, not yet separated (a stable-branch ring-ordering fix in `linux-7.1.y`, absent from the dev host's kernel) — an operator decision on the host kernel, not made. u8:i4 beyond about 98k tokens under the bypass stays unsafe on fault-reporting cards; at bound 32 with the 165,680-token pool the 16 GiB card passed 5 of 5 runs on a quiet host and 4 of 7 while the host was also building plugin variants (host load not recorded) earlier that evening. The belt itself still costs about 12% of prefill at chunk 64/32 against chunk 128 on short prompts. Owed, unchanged: cold/warm prefix byte-exactness at u8:i4 (the INT3 sub-4-bit study belongs to M10, not here). |
| M9 | **Expert offload v2: feed-forward tensors on their own budget** — the FFN expert tensors are the offloadable class, dense/attention tensors are device-resident by priority; async batched host→device slot uploads (pinned staging, ordered queue), prefetch on router output, LRU persistent across requests | The measured blocker falls: slot-upload time overlapped with compute instead of 309 µs/tensor synchronous (13.1 s per 16 tokens at ratio 20 today); 35B q4 on the A770 at ratio 20 reaches ≥ 8 t/s decode **or** the item closes with a profile naming the next blocker; LRU hit rate reported per run. **2026-09-02: substantially met** — patches/0004–0007 (device-tier pool + async uploads + finish-gating): 0.4 → 9.1 t/s at ratio 50 with an 8 GiB device pool (4.9 at ratio 25); the dial verdict of §7.0.2s overturned (§7.0.2v). Counters read after the flush fix: avg copy 309→5 µs/tensor, tensor_loads = misses×9 (reconciliation resolved), evictions 0 (hot-set pin deferred by the design's own rule). Owed: equivalence+Prüfstand gates before rollout. **2026-09-02:** Prüfstand discharged on the 0001–0012 series with the offload path live — the 35B at ratio 50 with the M14 tier (§7.0.2x) and the coder at ratio 20 / 8 GiB device pool / u8:i4 at 10/10, twice with identical answers (§7.0.2y). Equivalence in this repository's sense (cold/warm, one/two lanes, paged/stateful) is still owed for the offloaded path. **2026-09-04, gates run, one axis fails (DESIGN §7.0.2ae).** 24 GB card, `marfrit-openvino +p3`: the coder at `--offload-ratio 20 --paged-kv u8:i4` passes every equivalence and concurrency check. The 35B at `--offload-ratio 50 --moe-cpu-tier` passes concurrency and every equivalence check but the continuation one, which fails 3 of 3 with the same one-token fork: the host tier's expert LRU residency is process-global and selects device-f16 versus host-f32 arithmetic (not bit-equal), so greedy output depends on request history — a real §3.4 violation, not filed beside it. Shipped today: `--moe-cpu-tier` refuses `--prefix-cache-mib > 0`. This refusal removes only the cross-restore axis; §3.4 stays violated with the tier on until F1 (patch 0018). Fix of record: promote patch 0011's divergence oracle to the served host kernel as plugin patch 0018 (cost not yet measured); two discriminators (tier OFF, and history vs. restore) are running. **E1 measured:** the identical suite with the tier off (`--offload-ratio 50` alone) passes every check, the continuation included — the tier is the discriminator. **E2 measured:** with the tier on and the prefix cache off, one process asked PROMPT, PROMPT, CONT gives the same answer to PROMPT twice and a THIRD distinct continuation for CONT — equal neither to the cold nor to the restored text — so the tier is history-sensitive in general; the cache and the restored state are exonerated, and the reading above stands as measured. **2026-09-04, patch 0018 verified:** two further load-time bugs fixed (the device-pool probe and the prefill fallback path both assumed an evictable/acquirable slot always exists, false under a 100%-pinned static partition; the refusal gate itself queried the static-partition property on the wrong object, before compile, which always throws — DESIGN §7.0.2ae). Two more followed in review after that run — the `ARCINT_FIT_SLOT_BYTES` override could bypass the refusal entirely, and the plugin-side property never checked whether the tier was on — five in all (§7.0.2ae). With all five fixed, the continuation-restore check passes on real hardware. One unrelated check flaked in the same run (n-gram drafter determinism, no MoE routing) — open, not yet characterised. **2026-09-05, closed at the 0.3.0 release gate (DESIGN §7.0.2ai).** On the shipped admission path with the built `+p4` package: the coder at this row's cell (`--offload-ratio 20 --paged-kv u8:i4`, 24 GB card) passes the equivalence suite at one and two lanes and the concurrency suite; the 35B tier cell passes E2 (a continuation after in-process history is byte-identical to a fresh process's) and tier ON is byte-identical to tier OFF; the gate found and fixed a sixth load-time bug (the static partition's device pool charged at its full analytic size refused the tier-ON cell on the 16 GiB card — the probe now runs under the static partition too); and the n-gram flake did not reproduce in six fresh processes on the tier configuration, six identical outputs — one failure in about nine runs on the record, kept as a rare event, not a mechanism. §3.4 holds on the offloaded path; the row closes. |
| M10 | **Sub-4-bit expert weights** — mixed per-tensor quantisation: rarely-routed experts below int4 (Q3-class ~3.2–3.5 bpw), dense and shared tensors at int4/int8; route is NNCF mixed-precision export or GGUF K-quant import with dequant-to-int4-at-load, decided by measurement | A mixed artifact serves with ≥ 15% more max context than pure int4 on the same card at Prüfstand 10/10; per-expert bpw map recorded in the artifact; quality delta vs int4 measured, not asserted. **2026-09-05, re-scoped for 0.3.0 (DESIGN §7.0.2ah).** The context claim is discharged by M8's u8:i4 flag (+28.3% / +28.4% measured, §7.0.2y). The sub-4-bit expert path is a new GPU-kernel milestone — the pinned runtime's MoE fusion matches u4 only, its kernels carry no sub-4-bit type, and the gate is priced in VRAM, which neither route as written reaches without that kernel — backlogged to 0.3.1 (see Backlog below). No code written; the recon is on the record in §7.0.2ah. |
| M11 | **Drafting II: trees from marginals** — a Weaver-class conditional re-ranker (~57M adapter, top-K-restricted, trained per the TfM recipe) over the served DFlash2 head; upstream PA tree-mask (2026.2) for the attention layers; a GDN tree-verify (masked triangular solve over the ancestor partial order) as a plugin patch — **the state is never speculatively written; the accepted path is replayed at commit**, which is the invariant our chain integration already holds; an ngram/suffix table drafter for the drafter-less endpoints | Chain re-rank first: tokens/verify-cycle > the measured 3.13 (B60, int4, block 8) at byte-exact greedy equivalence; tree path after: τ and t/s vs chain on the same card, or a documented negative with the profile. Also closes the 0.2.x leftovers: acceptance under the thinking template and under prefix-cache hits, and a Prüfstand run with the drafter on. **2026-09-03 — measured (DESIGN §7.0.2z).** Baseline reproduced (3.28 / 3.12 tokens per cycle, 44.6 / 45.0 t/s), Prüfstand 10/10 with the drafter on, the thinking template at 2.02 per cycle, a warm prefix hit keeps the drafter. Free levers measured: Viterbi over the selector's lattice is a negative on every probe (the score is not a path objective), longer blocks trade +3–8% tokens per cycle for −6–21% throughput, the ngram drafter falls below plain decoding on this model (10/10 kept). The offline oracle puts a floor of +0.74 accepted tokens per cycle under the re-rank track (the free levers took none of it) and +0.43 under a realizable width-4 tree; the adapter (a selector re-fit, five to eight days, CPU training) is the operator's decision with that floor, the tree path is not pursued (its bound sits below the re-forward it carries and it needs a new paged-attention input). Landed: --dflash-block, --dflash-select greedy|viterbi (greedy default, byte-identical), --dflash-lambda, --dflash-topk, the cycle dump and tools/dflash_oracle.py. **2026-09-03, depth (§7.0.2aa):** DFlash is below plain decoding at every depth measured (8.9k to 76k) and the MTP head from 37.7k (at 76k MTP 1.0 t/s, DFlash 5.3, plain 16.3) and accept nothing at 76k; neither zero is explained yet (an MTP step profile at depth is owed); DFlash past 2,048 tokens fixed (patch 0014 and a recoverable, capped drafter). Deep contexts serve without a drafter until then. **2026-09-04, the step profile, named (DESIGN §7.0.2ag).** New per-cycle instrumentation (`ARCINT_PROFILE_CYCLE`) on the dense 27B agent, 24 GB card: at 8.9k MTP holds 85% acceptance (7.3 t/s), DFlash 73% (6.0 t/s); at 76k both fall to 0% and the plain arm itself is unchanged, 15.3 t/s at both depths — the earlier "445 vs 209 ms" comparison (§7.0.2aa) is RETRACTED as not like-for-like. Two mechanisms, marked as the design note's own reading pending a verification window: MTP's unpaged KV state (8 KiB/token, primed over the whole prompt) is not in the reservation and overcommits the card past its own logged ceiling at depth, which reads as the source of its bimodal, spiking verify cost; both drafters compute rotary in-graph under the plugin's default f16 precision, which overflows at 65,504 — a measured cliff (60,333 tokens 78% accepted, 70,403 tokens 0%), not a decay. Fixed, arcint side: an MTP-state reservation term, `ARCINT_DRAFT_F32=1`, and rotary-subgraph f32 marking on by default (`ARCINT_DRAFT_ROPE_F16=1` reverts it). The gate is restated: 3.13 tokens/cycle is DFlash's own break-even, not MTP's, which can accept at most 2 tokens/cycle and breaks even on cycle wall alone (~130 ms at 76k). **Verification window measured.** At 70,403 tokens: rope fix off (`ARCINT_DRAFT_ROPE_F16=1`) 0/148 accepted (the eviction spikes are gone, verify 581→157 ms, but acceptance stays zero — the two mechanisms are separable); MTP default 80.5% accepted (66/82), 7.9 t/s, byte-identical; `ARCINT_DRAFT_F32=1` restores acceptance (1/cycle) at a prohibitive 6.3 s/cycle (the f32 state re-crosses the ceiling); DFlash default 27.5% accepted (98/357), 15.5 t/s against plain's 15.3. At 77,134 tokens with the MTP-state term charged, n_ctx clamps to 127,536 (was 155,488 unclamped): MTP 90.8% accepted (177/195) but still only 4.9 t/s (cycle ≈390 ms against its own ~130 ms break-even — MTP loses to plain even at full acceptance); DFlash 40.6% accepted (276/679), 18.8 t/s, ~4.4 tokens/cycle — the M11 gate (>3.13 tokens/cycle, byte-exact) PASSES with DFlash at 76k. Decision input, a recommendation not a change made: MTP never beats plain on this artifact at any measured depth; DFlash loses shallow, wins deep — DFlash with a depth gate (or plain below the crossover), MTP off. **2026-09-05, closed at the 0.3.0 release gate (DESIGN §7.0.2ai).** The dense agent on the 24 GB card, release binary and the built `+p4` package: the equivalence suite's MTP section — M11's shallow gate — passes (MTP identical to plain greedy; MTP with the prefix cache, warm equal to cold with a real hit; the head accepting 68.4% at 8k), the cold/warm section and the concurrency suite pass, and the suite completes with its chunk-1 sweep included after the gate found and fixed a load-time refusal there (`--prefill-chunk 1` on an MTP artifact: the logits-slice check expected the verifier's two rows from a one-token probe forward; `logits_slice_rows_expected`, red-first). The 76k cell of the verification window above was not re-run: nothing since touched it except gating the MTP-state term on the device it runs on, inert in the served configuration. The row closes on the verdict above; the deep-context serving choice stays the operator's. |
| M12 | **Host-loop and exporter** — pin the dispatch thread; lower the exporter's MoE to the per-expert pattern upstream `fuse_moe_experts` matches, so the fused path (and upstream's batched-MoE decode work) applies to our IRs | `fuse_moe_experts` proven to fire on an exported IR (graph dump); M=2 verify host cost re-measured against DESIGN §7.0.2p's numbers; per-cycle host ms delta named. **2026-09-02, corrected on-card:** the pass that matters is `ConvertTiledMoeBlockToGatherMatmuls` (tiled/compressed contract, §7.0.2u), proven ×40 in the served 35B's graph; the exporter emits the tiled form (`--moe-lowering tiled`) and the real head exports and compiles at production dims (946 MB u8 vs 1.69 GB f16 batched) — fused-op demonstration outside the engine's own base-model compile still open (one named discriminator). Dispatch pinning measured a null on a quiet host (`--pin-dispatch` kept for the contended arm) |
| M13 | **Vision projector disabled** — load the `*ForConditionalGeneration` IRs with the vision tower and projector excluded (they are in every checkpoint we serve); reserve `--vision` for later; pay zero VRAM for the modality we don't serve | Multimodal IR loads with vision excluded; text path byte-identical to the text-only export; VRAM delta of the excluded tower measured; request parser still rejects image parts explicitly. **2026-09-02 — closed, with one criterion not run (DESIGN §7.0.2y).** The loader never resolved the vision IRs (artifact resolution names only the language model and the text embeddings), so "vision excluded" and "zero VRAM" hold by construction, not by a measured delta; what was missing was the record and the guard. Now: `--vision` is reserved and refused; the artifact scan reports the present-but-unloaded vision IRs at load (coder: 6 files, 428.3 MiB on disk; a first version of the scan under-counted them and the runtime line caught it); a process-level fd/maps check was tried and dropped as evidence (after compile the process holds no mapping on the language-model file either, so it cannot discriminate); the parser's rejection of image parts is tested. Not run: the byte-identity against a separate text-only export, which needs a second export of a served checkpoint (hours, ~15 GB) — the language-model IR *is* the text path of the VLM export; a scope call, flagged |
| M14 | **CPU compute tier** — kernels vendored from a license-compatible high-performance CPU engine (candidate of record: ik_llama.cpp's iqk GEMM/GEMV kernels, MIT — compatible with this repository's Apache-2.0 under the `THIRD_PARTY.md` convention), tuned for the Zen 3 host class (Ryzen 7 5700X: 8C/16T, AVX2+FMA3, **no** AVX-512 and no AVX-VNNI, so the int8 path is the `maddubs` AVX2 one, not `HAVE_FANCY_SIMD`; 32 MiB L3; dual-channel DDR4 ≈ 50 GB/s ceiling). Primary use: **offloaded expert FFNs computed in place on the host** — the `--n-cpu-moe` analog — so M9's spill class stops crossing the bus at all; secondary: a full-CPU fallback engine for drafter-less and card-down operation. Ties into M10: the iqk kernels natively compute the K-quant/IQ_K sub-4-bit formats a mixed artifact would store | Expert-FFN-on-host beats M9's upload path on the same 35B ratio-20 config (per-token expert compute vs measured 309 µs/tensor uploads, profile recorded); full-CPU Prüfstand 10/10 on one model with t/s reported against the DDR4 bandwidth ceiling, not against GPU numbers; vendored files land in `third_party/` with license and version pinned in `THIRD_PARTY.md`. **2026-09-02 — scoped, gate redefined.** Plugin recon: the decode hot path is one fused GEMV over all (token,expert) pairs (`exec_batched_gemv`); the per-expert loop exists only on the prefill/fallback path; "offloaded" is not a fixed set but whichever experts miss the slot LRU this token. The tier is therefore an in-plugin **hybrid decode split** at the LRU miss (skip-mask in the fused kernel, host x→y excursion riding the existing per-layer topk readback, combine in the unchanged reduce). Two decisions on the record: (1) the CPU kernels are written for the plugin's *actual* grouped-int4 layout — no repack, no GGUF/M10 coupling, ik_llama.cpp's iqk techniques credited rather than vendored (their headline speed is on K-quant blocks the plugin does not store); (2) **llama.cpp on CPU is not the bar** — arcint's win is the GPU path; M14's gates are equivalence (host-only reference test + acceptance 10/10) and an honestly reported decode number against M9's device-tier path on the cold tail, win or lose. Envelope (hypothesis, to be measured): 2.5 MB int4 expert ≈ 50 µs from DDR4 vs ≈ 175 µs over PCIe; the CPU compute sits on the per-layer serial path, which M9's async uploads do not. **2026-09-02 — measured, the tier wins (DESIGN §7.0.2x).** 35B int4 on the 16 GiB card, u8 KV, one lane, n_ctx 65,536, 64 greedy tokens: tier ON 15.0/15.5 t/s vs OFF 10.4/10.6 at the reference cell (ratio 50, 8 GiB device pool — not this row's ratio 20, which spills resident experts into GTT on this card at 2.1 t/s; the pre-registered rule picked the largest evicting pool at the fastest OFF ratio), 14.1/14.8 vs 7.4/7.5 at ratio 75 / 5 GiB; greedy text byte-identical OFF vs ON at both cells; acceptance 10/10 with the tier on; fused ×40 with the tier on; kernel-skip red case run on a card. Three measured defects on the way (a `clone()` that dropped the tier's members, the AVX2 TU compiled at -Os by the library's flag order, and the envelope's bandwidth assumption); the host kernel sits at 198 µs/expert from RAM. Full-CPU fallback engine and `THIRD_PARTY.md` vendoring dropped from this milestone by the scoping decision above; the next named lever is the per-layer host readback round trip. **2026-09-04, equivalence gate: one axis fails, on the record at M9 (DESIGN §7.0.2ae).** The tier's own device/host kernel split makes expert-slot LRU residency arithmetic-selecting, not merely placement-selecting — device f16 versus host f32, not bit-equal — so greedy output with the tier on depends on the process's own request history across a prefix-cache restore; M14's own gates (host-only reference + acceptance 10/10) did not exercise that axis. Ship decision and fix of record tracked on M9's row; the tier otherwise stands as measured above. **2026-09-04, the readback decomposed (DESIGN §7.0.2af).** Patch 0017 (counters only, plus usm_host readback destinations and a topk_id/x/routing-weight hoist into one wait) found the 283 microsecond-class "readback" cost is queue backlog — the host tier waiting for the GPU to reach the same layer's router, not a 4 KiB transfer: a stream.finish() drain probe timed before the read costs 3,578 microseconds on its own, and the read after the drain then waits only 107. The candidate next lever named at §7.0.2x (host-visible residency for the decode input) cannot remove an event wait and is retired before being built. This window's own decode rates (16 GiB card, four processes) ran about 3x below the record and were not comparable to it; a "page cache cold" reading from that window is RETRACTED (the container's root is on ZFS, whose file data lives in the ARC, not in what `fincore` reports). A clean rerun (quiet host, both production units stopped, six processes: pre-0017 ON, 0017 ON, pre-0017 ON again, 0017 ON with the topk_id hoist disabled (NOHOIST), 0017 ON with the drain probe, 0017 OFF) confirms §7.0.2af's conclusion in one build rather than by inference: with the hoist off the blocking topk_id read is counted on its own at 3,620 microseconds (the same queue backlog pre-0017 paid uncounted) and the usm_host readback that follows costs only 53; hoisted, the merged wait is 3,651 — the hoist itself is a null, 5.0 t/s either way, and the usm_host destination change is the whole of 0017's +4% over pre-0017. All six arms were byte-identical over the 64 tokens this time, including pre-0017 — this sample's residency history happened not to fork, unlike the M9 continuation cell, consistent with §7.0.2ae's own reading rather than against it. RETRACTED: every decode rate in this window and its clean rerun is void as a comparison to the record. Every process here ran the plugin's own default device pool, not the intended 8 GiB one, because `ARCINT_MOE_DEVICE_POOL_BYTES` (the env var arcint has set it with since M7) was never exported this window — the expert slots lived in host memory (GTT) throughout, the same GTT-spill regime the 2026-09-02 record already describes and prices (its own "ratio 20 spills into GTT, 2.1 t/s" cell). The in-build comparisons above (usm_host, the hoist null, the queue backlog, byte-identity) stand — every arm shared the same wrong pool. Lesson for the record: every tier window must export `ARCINT_MOE_DEVICE_POOL_BYTES`. REPRODUCED at the true pool (solo run, both production units stopped, files pre-read): tier OFF 9.2/9.4 t/s, tier ON 11.0/15.3, byte-identical to each other — within the run-to-run spread of the 2026-09-02 record (10.4/10.6 / 15.0/15.5); the discrepancy is closed. **2026-09-05:** the oversized `routing_weights_size = n_token * max_topk` declaration flagged beside the patch 0018 fix in the tier's own prefill fallback (DESIGN §7.0.2ae) is investigated and closed as a harmless upstream over-declaration — the consumer is a per-row binary post-op whose descriptor is fixed at creation to exactly `n_token`, the inflated extent is never read nor inspected by oneDNN, and it always sits inside the `max_topk * token_num` allocation; no change carried. **2026-09-05, closed at the 0.3.0 release gate (DESIGN §7.0.2ai).** Re-measured under patch 0018's static partition on the shipped admission path — after the gate itself found and fixed an over-charge that refused this very cell on the 16 GiB card (the device slot pool priced at its full analytic 7.50 GiB where the driver keeps it host-mapped and the probe reads 0.11 GiB): decode holds at the record, 16.4 t/s on the second request of both processes (15.4 already on the first request of the warm-cache one) against tier OFF's 11.3–11.4 on the same request in the same window; tier ON is byte-identical to tier OFF and E2 passes; prefill loses 3× (26.6–26.7 vs 87.4–87.6 t/s on the second request, every layer's prefill on the per-expert fallback, `grouped_fallbacks` one per layer); the first processes of a cold sequence pay a one-time warming (loads of 215–585 s against tier OFF's 30–45, first requests 0.1–4.2 t/s decode) that later processes do not. The M14 gate's honest reading: the decode win stands under the partition that makes it §3.4-clean; the prefill and cold-start costs are 0.3.1's ("the static partition's prefill and cold start", Backlog below). |

Ordering intent, not a promise: M7 and M12 first (they make every later
measurement honest and cheaper), M8/M9 next (memory levers), M10/M11 after
(they consume the levers), M14 alongside M9/M10 (it is the other half of the
expert-placement question and shares M10's quant formats), M13 whenever an
export forces the question.

## Backlog for 0.3.1

Carried out of 0.3.0 by decision, not by running out of time; each entry
keeps the gate it was written with and names what has to exist before it
can be picked up.

| item | origin | gate | entry criteria |
|---|---|---|---|
| **VRAM-resident sub-4-bit expert weights** — a new GPU kernel path: a fusion matcher for the new element type (the pinned runtime's `keep_moe_3gemm_const_precision` matches `u4` only), an oneDNN bypass (no 3-bit type there), a GEMV/GEMM with in-kernel dequant, judged by a fusion-impact profile (ground rule 2), with a decode regression of known sign (DESIGN §7.0.3's u4-KV precedent) | M10, re-scoped 2026-09-05 (DESIGN §7.0.2ah) | the M10 row's own gate, unchanged: a mixed artifact serves with ≥ 15% more max context than pure int4 on the same card at Prüfstand 10/10; per-expert bpw map recorded in the artifact; quality delta vs int4 measured, not asserted | the recon on record (§7.0.2ah); the `INT3_SYM` `compress_weights` smoke test against a real IR (NNCF 3.3.0 is in the tooling venv; clears §7.0.2y's "undocumented" caveat); a routing histogram (patch 0013) over a corpus long enough that "rarely-routed" has a distribution to threshold on; the per-expert bpw map's artifact format decided (sidecar, XML attribute or per-expert constant naming — the unrolled export form makes the last natural, the batched form does not); the resident format (`u3` group-quant vs K-quant blocks) picked by measurement on one expert layer, same f16 source, same calibration, against the int4 baseline |
| **K-quant storage for the host compute tier** — the M14 tier's host kernel computing Q3_K/IQ3-class blocks natively, so offloaded experts are sub-4-bit on disk and in the host pool (VRAM untouched) | M14 extension, named in the M14 row's own text; separated from the item above by §7.0.2ah | an honestly reported host-pool byte count and decode number against the grouped-int4 host kernel at the M14 reference cell, win or lose; byte-exact equivalence tier on vs off as today | the item above's resident-format measurement (it decides whether K-quant blocks are the format at all); the `iq3xxs_grid` and sibling constants vendored under the `THIRD_PARTY.md` convention (MIT) |
| **The static partition's prefill and cold start** — patch 0018's F2 partition is §3.4-correct and, warm, holds M14's decode result at the reference cell (the 0.3.0 release gate, DESIGN §7.0.2ai: 16.4 t/s decode on the second request, at the LRU-era 15.0/15.5 and above tier OFF's 11.3–11.4 on the same request). Two costs remain, both measured there: prefill runs at 26.6–26.7 t/s against tier OFF's 87.4–87.6 on the same request, because every layer's prefill takes the per-expert fallback (`grouped_fallbacks` = one per layer — the grouped-GEMM path refuses a batch with any non-resident expert, and under a static half every batch has one); and the first processes of a fresh sequence pay a one-time warming (loads of 215–585 s against 30–45, first requests at 0.1–4.2 t/s decode, 325 oneDNN kernels created per process, `created_onednn_kernels`) that later processes do not — a cache effect across processes, not separated further. Levers, all history-independent: a grouped prefill that runs the resident subset on device and only the non-resident subset on the host instead of abandoning the whole layer; a persistent kernel cache or explicit pre-warm so a fresh sequence's first request is not the slow one; and, only if decode variance survives those, seeding the partition from a fixed calibration routing histogram (patch 0013's counters over a corpus, frozen into the artifact or the config — a pure function of the seed and that table, never of the process's own history) so the pinned half is the hot half | M9/M14, found by the 0.3.0 release gate | tier-ON prefill within 25% of tier OFF at the reference cell, and the first request of a cold sequence within 2× of a warm one, with decode still at the record and the continuation-restore and E2 checks still passing — the §3.4 gate and the M14 gate held together, not traded | the 0.3.0 gate's own numbers as the baseline; the warming's owner identified (kernel cache vs. page cache vs. first-use fills) before anything is built |
| **Unit tests and acceptance tests differentiated** — today `arcint-test` under `ctest` mixes device-free unit cases with cases that only mean something on a card or a particular host (the CPU-affinity pin, the OpenVINO-backend cases), and the acceptance set (Prüfstand, the equivalence and concurrency suites, the per-milestone cells) is run by hand from DESIGN §5.1's table. Split them so a device-free `ctest` is the unit gate, a second target is the card-requiring acceptance set with its cells enumerated in one place, and a release checklist is generated from that enumeration rather than re-read from prose | operator direction, 2026-09-05 | `ctest` green on a machine with no card and no OpenVINO means exactly the unit set; the acceptance target lists every §5.1 cell by name and exits non-zero when one is skipped without being marked as such; the 0.3.1 release runs from that list | DESIGN §5.1's ladder as the spec; the 0.3.0 release gate's window scripts as the first acceptance-target contents. **2026-09-05, Increment 1 landed** (`docs/design-0.3.1-test-ladder.md`): bare `ctest` is the unit set by construction (`ARCINT_ACCEPTANCE` registers the cells, refused without the OpenVINO backend or the tests); `tests/acceptance/cells.json` enumerates twelve cells, `tests/acceptance/run.py --all` fails on any skip not named, `tools/acceptance_manifest.py` generates the §5.1 row and `docs/release-checklist.md` with a check that runs as a unit test; the harness skips visibly under `--max-skips 0`. Review caught six cells handing their flags to suites that drop positionals past the device — the wrongly-green shape itself — before commit. Owed: Increment 2, the four runners marked as such in the enumeration, validated in one card window |

## Deliberate exclusions

- **Tensor parallel / multi-GPU disaggregation** — the two cards serve two
  models; the state-transfer contract note (DESIGN §8) stands for the day this
  changes.
- **MLA/FlashMLA** — no DeepSeek-family target in the allowlist.
- **Sampler zoo** (XTC, top-n σ, adaptive-p) — the operator layer covers
  serving defaults; exotic samplers are demand-driven, not planned.
- **Bitnet** — no artifact, no demand.

## Provenance

The feature set collects every open thread on the record: DESIGN §8's
deferrals (KV codec, MoE placement, host tier, multimodal), §7.0.2s's offload
verdict ("a fit lever, not a context dial" — M7/M9 are the two halves of
making it a context dial after all), the enqueue-bound follow-ups (M12), the
DFlash serving leftovers and the Trees-from-Marginals result (M11,
arXiv:2607.06763), and the fit/deferred-commit/replay, asymmetric-KV,
FFN-offload, projector-disable and LRU techniques surveyed from the
llama.cpp/ollama fitting work and ik_llama.cpp (M7–M9, M13). M14 vendors
rather than reinvents: the donor must be license-compatible (MIT and
Apache-2.0 both qualify; ik_llama.cpp is MIT, copyright ggml/llama.cpp/
ik_llama.cpp authors), carried under the existing `THIRD_PARTY.md` rules with
licenses unchanged.
